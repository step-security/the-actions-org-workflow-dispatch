name: Yarn Audit Fix Run

on:
  workflow_dispatch:
    inputs:
      base_branch:
        description: "Specify a base branch"
        required: false
        default: "main"
      node_version:
        description: "Node.js version to use"
        required: false
        default: "24"
      script:
        description: "Command used to build/bundle the action"
        required: false
        default: "yarn build"
      package_manager:
        description: "Package manager to use"
        required: false
        default: "yarn"
      yarn_version:
        description: "Yarn version to use"
        required: false
        default: "4.18.0"
  schedule:
    - cron: "0 0 * * 1"

jobs:
  audit-fix:
    uses: step-security/reusable-workflows/.github/workflows/audit_fix.yml@v1
    with:
      force: ${{ inputs.force || false }}
      base_branch: ${{ inputs.base_branch || 'main' }}
      node_version: ${{ inputs.node_version || '24' }}
      script: ${{ inputs.script || 'yarn build' }}
      package_manager: ${{ inputs.package_manager || 'yarn' }}
      yarn_version: ${{ inputs.yarn_version || '4.18.0' }}

permissions:
  contents: write
  pull-requests: write
  packages: read
  issues: write
